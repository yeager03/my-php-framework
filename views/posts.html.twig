{% extends "default.html.twig" %}


{% block main %}
    <div class="container">
        <h1>My posts</h1>
        <ul>
            {% for post in posts %}
                <li>{{ post.title }}</li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}